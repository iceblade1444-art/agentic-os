{
  "name": "agentic-os",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agentic OS — dashboard + backend (LLM proxy, MCP host, integrations)",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  }
}
